import { createStore } from 'vuex'
import wallet from './modules/wallet'

export default createStore({
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        wallet
    }
})